---
description: Evaluate a proposed skill against the force-multiplier bar via the qualify agent.
argument-hint: "<skill name, description, or draft SKILL.md>"
---

Dispatch the `qualify` agent via the Agent tool with `subagent_type: qualify`, passing $ARGUMENTS as the candidate.

If $ARGUMENTS is empty, ask the user to paste the candidate's name, description, or full SKILL.md draft before dispatching.
